info:
  name: Get Styles
  type: http
  seq: 1

http:
  method: GET
  url: "{{TILESSERVICE_HOST}}:{{TILESSERVICE_PORT}}/v1/tiles/styles"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
